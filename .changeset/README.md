# Changesets

Add a changeset for a user-visible change to `@deepwork/domain`,
`@deepwork/sdk`, or `@deepwork/ui`:

```bash
pnpm changeset
```

The packages remain private until a separate release review authorizes
publication. Changesets establish compatible version history without publishing
or tagging private packages.
