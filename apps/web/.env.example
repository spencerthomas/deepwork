# The default is http://127.0.0.1:8000 when this variable is omitted.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Explicit local fallback only. The UI displays a persistent fixture-mode banner.
# NEXT_PUBLIC_DEMO_MODE=fixture
