packages:
  - "."

overrides:
  "@napi-rs/wasm-runtime": "1.1.6"
  "baseline-browser-mapping": "2.11.0"
  "enhanced-resolve": "5.24.3"
  "vite": "8.1.5"

allowBuilds:
  # The web app does not use image optimization, so the optional native build
  # remains disabled just as it is in the repository-level pnpm workspace.
  sharp: false
