import { SchedulesView } from "@/components/schedules/schedules-view";

export const metadata = {
  title: "Schedules — Deep Work",
};

export default function SchedulesPage() {
  return <SchedulesView />;
}
