# Staged agent-guidance evidence

These files preserve the proposal drafts. Active instructions exist only where a
canonical directory currently exists. Root and documentation guidance is installed
at `AGENTS.md` and `docs/AGENTS.md`; future package files are installed with their
scaffolded directories after reconciliation. The sibling frontend draft is never
installed by this repository.
