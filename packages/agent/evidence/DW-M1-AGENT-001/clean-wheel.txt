Recorded: 2026-07-23 AEST
Command: make -C packages/agent package-check
Result: PASS

The verifier built the wheel and source distribution, created a clean temporary
Python 3.12 virtual environment, removed PYTHONPATH, installed the wheel with
--no-index and --no-deps, imported every declared public export, found py.typed,
confirmed runtime_available=false and SPIKE-CONFIG-001, built a second artifact
pair in a separate temporary directory, required identical hashes, compared those
fresh hashes with the immutable artifacts.json manifest, and removed both
temporary directories. Ordinary package-check does not rewrite evidence.
