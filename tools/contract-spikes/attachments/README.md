# Attachment contract spike

This isolated project validates `SPIKE-ATTACH-001` without enabling product
attachment capability. Its ordinary tests and commands deny network access and
use only harmless synthetic bytes.

The deterministic adapters prove the local quarantine and transfer state machine,
not an object-store, malware-scanner, model-provider, or hosted Classic LangSmith
Deployment contract. Live-required matrix rows remain
`blocked-live-evidence` unless all sanctioned non-production dependencies are
explicitly supplied.

Run the exact commands retained in
`docs/references/research/attachment-contract-spikes/commands.txt` from the
repository root.
